spring.application.name=postech-fastfood

# Enable ansi output
spring.output.ansi.enabled=always

# Spring Boot Configuration
spring.devtools.add-properties=true

# Server configuration
server.port=8080
server.address=0.0.0.0

spring.datasource.url=${DATASOURCE.URL:jdbc:mariadb://mariadb:3306/postech-fastfood}
spring.datasource.username=${DATASOURCE.USERNAME:fiap}
spring.datasource.password=${DATASOURCE.PASSWORD:fiap}
spring.datasource.hikari.pool-name=${DATASOURCE.POOL-NAME:postech-fastfood}
spring.datasource.hikari.maximum-pool-size=${DATASOURCE.MAXIMUM-POOL-SIZE:5}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.hikari.connectionTimeout=10000
spring.datasource.hikari.maxLifetime=1800000

# JPA config
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.format_sql=true

# Spring Locale
spring.web.locale=pt_BR
spring.web.locale-resolver=fixed

# Jackson Parameters
spring.jackson.serialization.write_dates_as_timestamps=false